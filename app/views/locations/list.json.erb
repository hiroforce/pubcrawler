{
<% @locations.each do |location| %>
		
		"Location": {
		"id:" "<%= location.id %>"
		"name": "<%= location.name%>"
		"address": "<%= location.address%>"
		"type": "<%= location.bar_type%>"
		"subtype": "<%= location.subtype%>"
		"description": "<%= location.description%>"
		"icon": "<%= location.icon%>"
		"distance": "<%= location.distance %>"
		}
<% end %>
}	